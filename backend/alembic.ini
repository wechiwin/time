[alembic]
# Path to the migration script directory
script_location = migrations

# A base module where your models are imported from.
# This is crucial for autogenerate to find your models.
# Example:
# sqlalchemy.url = postgresql://user:password@host:port/dbname
# Or, as we'll do, let env.py handle it from Flask config

# ... (其他配置，通常不需要修改) ...
